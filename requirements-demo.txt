# Python deps for the Streamlit demo container
# (Cloud-only mode — no torch/transformers needed; inference goes to Vast.ai)
streamlit>=1.30,<2
numpy>=1.24
soundfile>=0.12
requests>=2.31
mlflow>=2.10
boto3>=1.34
librosa>=0.10
