# Python deps for the Streamlit demo container
# Supports both local (torch) and cloud (Vast.ai) inference modes
streamlit>=1.30,<2
numpy>=1.24
soundfile>=0.12
requests>=2.31
mlflow>=2.10
boto3>=1.34
librosa>=0.10

# Local inference (torch CPU — no CUDA in Docker on macOS)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1,<3
transformers>=5.0,<6
accelerate>=0.25
